pipeline {
agent any 
stages {
        stage('Compile') {
            steps {
            bat 'javac hello.java'
            }
        }
         stage('Run') {
            steps {
            bat 'javac hello'
            }
        }
    }
}
